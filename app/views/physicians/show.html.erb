<p style="color: green"><%= notice %></p>

 <table border="1">
    <tr>
    <th>Patient Name</th>
    <th>Patient email</th>
    <th>Appointment Date</th>
    </tr>
   <% @appointments.each do |appointment| %>
    <tr>
   <td> <%= Patient.find(appointment.patient_id).name %></td>
   <td> <%= Patient.find(appointment.patient_id).email %></td>
    <td><%=appointment.appointment_date.to_time%></td>  
   </tr>
  <% end %>
   </table>
<div>
  <%= link_to "Edit this physician", edit_physician_path(@physician) %> |
  <%= link_to "Back to physicians", physicians_path %>

  <%= button_to "Destroy this physician", @physician, method: :delete %>
</div>
