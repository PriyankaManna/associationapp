add sector
<%= form_with(model: @industries_sector) do |form| %>
  <% if @industries_sector.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@industries_sector.errors.count, "error") %> prohibited this industry from being saved:</h2>

      <ul>
        <% @industries_sector.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Industry, style: "display: block" %>
    <%= form.select(:industry_id, options_for_select(@industry.all.map { |u| [u.name, u.id]}) ) %>

  </div>

  <div>
    <%= form.label :Sector, style: "display: block" %>
    <%= form.select(:sector_id, options_for_select(@sector.all.map { |u| [u.name, u.id]}) ) %>

  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
